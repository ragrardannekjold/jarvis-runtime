name: Relay A7 (Shared Token Disabled)

on:
  workflow_dispatch:

permissions: {}

jobs:
  relay-a7-disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Shared-token lane is quarantined
        run: echo "Disabled pending migration to an isolated short-lived credential."
