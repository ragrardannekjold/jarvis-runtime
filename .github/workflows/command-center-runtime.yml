name: Command Center Runtime (Shared Token Disabled)

on:
  workflow_dispatch:

permissions: {}

jobs:
  command-center-runtime-disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Shared-token lane is quarantined
        run: echo "Disabled pending migration to an isolated short-lived credential."
