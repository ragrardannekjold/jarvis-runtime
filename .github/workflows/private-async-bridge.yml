name: Private Async Reference Bridge (Disabled)

on:
  workflow_dispatch:

permissions: {}

jobs:
  legacy-private-bridge-disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Legacy bridge is quarantined
        run: echo "Legacy V1 private bridge is disabled; use no private route until the signed v2 gate is activated."
