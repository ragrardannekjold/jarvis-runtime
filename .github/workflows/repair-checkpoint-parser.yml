name: Checkpoint Parser Repair (Retired)

on:
  workflow_dispatch:

permissions: {}

jobs:
  repair-checkpoint-parser-disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Shared-token lane is quarantined
        run: echo "Disabled pending migration to an isolated short-lived credential."
