# AI-49 main-based verification probe

This marker triggers the external verifier that already exists on the `jarvis-runtime` default branch.

Target under test: `ragrardannekjold/jarvis-command-center@ai-49-truth-nondegradation-runtime`.

Only the workflow logs and uploaded terminal readback count as verification. This marker is not a product result.
